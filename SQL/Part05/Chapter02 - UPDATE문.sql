-- UPDATE 문법
UPDATE
	TABLE_NAME
SET COLUMN_1 = VALUE1,
	COLUMN_2 = VALUE2
WHERE 조건
;

-- 실습 준비
ALTER TABLE LINK ADD COLUMN LAST_UPDATE DATE;
ALTER TABLE LINK ALTER COLUMN LAST_UPDATE SET DEFAULT CURRENT_DATE;
SELECT * FROM LINK;

UPDATE LINK
SET LAST_UPDATE = DEFAULT
WHERE
	LAST_UPDATE IS NULL
;

COMMIT;

SELECT * FROM LINK;

-- 전체 TABLE 수정
UPDATE LINK
SET REL = 'NO DATA'
;

COMMIT;

-- 전체 TABLE 수정 - 특정 컬럼을 이용
UPDATE LINK
SET DESCRIPTION = NAME
;

COMMIT;