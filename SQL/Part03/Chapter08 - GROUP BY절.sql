-- 문법
SELECT 
	COLUMN_1,
	집계함수(COLUMN_2)
FROM
	TABLE_NAME
GROUP BY
	COLUMN_1
;

--
SELECT
	CUSTOMER_ID
FROM
	PAYMENT
GROUP BY
	CUSTOMER_ID
;

--
SELECT
	CUSTOMER_ID,
	SUM(AMOUNT) AS AMOUNT_SUM
FROM
	PAYMENT
GROUP BY
	CUSTOMER_ID
ORDER BY
	SUM(AMOUNT) DESC
;

--
SELECT
	STAFF_ID,
	count(payment_id) AS count
FROM
	payment
GROUP BY
	staff_id
;
